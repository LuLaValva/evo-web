export interface Input extends Marko.HTML.TextArea {
    /**
     * If `large`, renders larger sized textarea.
     * @default "regular"
     */
    inputSize?: "regular" | "large";
    /** Textarea fills 100% of its container width. */
    fluid?: boolean;
    /** Only works with floating label. If set, then background is obscured of the floating label. Used to prevent label overlap */
    opaqueLabel?: boolean;
    /** If set then shows this text as the floating label. */
    floatingLabel?: string;
    /** Floating label will _always_ stay in the "up" position */
    floatingLabelStatic?: boolean;
    /** Indicates a field-level error with red border */
    invalid?: boolean;
}

<const/{
    class: inputClass,
    disabled,
    floatingLabel,
    floatingLabelStatic,
    fluid,
    id,
    inputSize,
    invalid,
    opaqueLabel,
    style,
    placeholder,
    readonly,
    onBlur,
    onFocus,
    ...htmlInput
}=input>

<id/textareaId=id>
<let/focused=false>
<let/value:=input.value>

<evo-floating-label|{ showPlaceholder }|
    floatingLabel=floatingLabel
    size=inputSize
    opaqueLabel=opaqueLabel
    alwaysUp=floatingLabelStatic
    htmlFor=textareaId
    disabled=disabled
    invalid=invalid
    fluid=fluid
    value=value
    focused=focused
>
   <${fluid ? "div" : "span"}
        style=style
        class=[
            "textbox" /** start remove after `:has` support */,
            disabled && "textbox--disabled",
            invalid && "textbox--invalid",
            readonly &&
                "textbox--readonly" /** end remove after `:has` support */,
            inputSize === "large" && "textbox--large",
            fluid && "textbox--fluid",
            inputClass,
        ]>
        <textarea
            ...htmlInput
            onBlur(e, target) {
                focused = false;
                (onBlur || null)?.(e, target);
            }
            onFocus(e, target) {
                focused = true;
                (onFocus || null)?.(e, target);
            }
            id=textareaId
            class="textbox__control"
            placeholder=showPlaceholder && placeholder
            value:=value
            disabled=disabled
            aria-invalid=invalid && "true"
            readonly=readonly/>
    </>
</>
