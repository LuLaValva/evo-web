export interface Input extends Omit<Marko.HTML.Input, "type"> {
    /** Sets the checkbox icon. Default is small. (Note: The dimensions of the checkbox will not change, but only the icon) */
    size?: "large";
}
<const/{ class: inputClass, size, ...htmlInput }=input>

<span class=["checkbox", inputClass]>
    <input ...htmlInput class="checkbox__control" type="checkbox">
    <span class="checkbox__icon" hidden>
        <if=size === "large">
            <evo-icon-checkbox-unchecked-24 class="checkbox__unchecked"/>
            <evo-icon-checkbox-checked-24 class="checkbox__checked"/>
        </if>
        <else>
            <evo-icon-checkbox-unchecked-18 class="checkbox__unchecked"/>
            <evo-icon-checkbox-checked-18 class="checkbox__checked"/>
        </else>
    </span>
</span>
